# Railway Environment Variables Template
# Copy and paste these commands into your terminal after connecting to Railway

# Core Application Settings (Required)
railway variables set DEBUG=false
railway variables set HOST=0.0.0.0
railway variables set PORT=8000
railway variables set API_TITLE="Blog Writer SDK API"
railway variables set API_VERSION="1.0.0"
railway variables set API_DESCRIPTION="A powerful REST API for AI-driven blog writing with advanced SEO optimization"

# Supabase Configuration (Required)
# Replace with your actual Supabase credentials
railway variables set SUPABASE_URL="https://your-project-id.supabase.co"
railway variables set SUPABASE_SERVICE_ROLE_KEY="your-service-role-key-here"
railway variables set SUPABASE_ANON_KEY="your-anon-key-here"

# AI Provider Configuration (At least one required)
# OpenAI
railway variables set OPENAI_API_KEY="your-openai-api-key"
railway variables set OPENAI_ORGANIZATION="your-openai-organization-id"
railway variables set OPENAI_DEFAULT_MODEL="gpt-4o-mini"

# Anthropic (Alternative to OpenAI)
railway variables set ANTHROPIC_API_KEY="your-anthropic-api-key"
railway variables set ANTHROPIC_DEFAULT_MODEL="claude-3-5-haiku-20241022"

# Azure OpenAI (Alternative to OpenAI)
railway variables set AZURE_OPENAI_API_KEY="your-azure-openai-api-key"
railway variables set AZURE_OPENAI_ENDPOINT="https://your-resource.openai.azure.com/"
railway variables set AZURE_OPENAI_API_VERSION="2024-02-15-preview"
railway variables set AZURE_OPENAI_DEFAULT_MODEL="gpt-4o-mini"

# CORS Configuration (Update with your frontend URL)
railway variables set ALLOWED_ORIGINS="http://localhost:3000,https://localhost:3000,https://your-frontend-app.vercel.app"

# Content Generation Settings
railway variables set DEFAULT_TONE="professional"
railway variables set DEFAULT_LENGTH="medium"
railway variables set ENABLE_SEO_OPTIMIZATION="true"
railway variables set ENABLE_QUALITY_ANALYSIS="true"

# Performance Settings
railway variables set BATCH_MAX_CONCURRENT="5"
railway variables set BATCH_MAX_RETRIES="2"
railway variables set RATE_LIMIT_REQUESTS_PER_MINUTE="60"
railway variables set RATE_LIMIT_REQUESTS_PER_HOUR="1000"
railway variables set RATE_LIMIT_REQUESTS_PER_DAY="10000"

# Logging
railway variables set LOG_LEVEL="info"
railway variables set LOG_FORMAT="json"
railway variables set METRICS_RETENTION_HOURS="24"

# Optional: DataForSEO (for enhanced SEO analysis)
# railway variables set DATAFORSEO_API_KEY="your-dataforseo-api-key"
# railway variables set DATAFORSEO_API_SECRET="your-dataforseo-api-secret"

# Optional: DeepSeek AI
# railway variables set DEEPSEEK_API_KEY="your-deepseek-api-key"
# railway variables set DEEPSEEK_API_BASE="https://api.deepseek.com"
# railway variables set DEEPSEEK_DEFAULT_MODEL="deepseek-chat"

# Optional: Redis (for caching)
# railway variables set REDIS_HOST="your-redis-host"
# railway variables set REDIS_PORT="6379"
# railway variables set REDIS_PASSWORD="your-redis-password"

# Optional: Monitoring
# railway variables set SENTRY_DSN="your-sentry-dsn"
# railway variables set ANALYTICS_ENABLED="true"

# Verify all variables are set
echo "Checking environment variables..."
railway variables

echo "Environment variables setup complete!"
echo "Next steps:"
echo "1. railway up (to deploy)"
echo "2. railway domain (to get your app URL)"
echo "3. railway logs (to view deployment logs)"
